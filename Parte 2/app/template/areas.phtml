<?php require __DIR__ . '/filtros.phtml'; ?>
<?php if ($agrupado === []): ?>
  <p>No hay cursos que coincidan con los filtros.</p>
<?php else: ?>
  <?php foreach ($agrupado as $grupo): ?>
    <section class="bloque-area">
      <h2><?= App\Core\View::e($grupo['area']) ?></h2>
      <?php foreach ($grupo['materias'] as $materiaGrupo): ?>
        <h3><?= App\Core\View::e($materiaGrupo['materia']) ?></h3>
        <div class="tarjetas">
          <?php foreach ($materiaGrupo['cursos'] as $curso): ?>
            <?php $schoolId = (string) $escuela['id']; require __DIR__ . '/card-curso.phtml'; ?>
          <?php endforeach; ?>
        </div>
      <?php endforeach; ?>
    </section>
  <?php endforeach; ?>
<?php endif; ?>
