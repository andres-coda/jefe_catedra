<?php
/** @var object $formulario Contrato: titulo, accion, datos, extra, borrar, aceptar. */
?>
<div class="conteiner">
  <h1><?= App\Core\View::e($formulario->titulo) ?></h1>
  <form method="post" action="<?= App\Core\View::e($formulario->accion) ?>">

    <?php foreach ($formulario->datos as $dato) {
        require __DIR__ . '/input.phtml';
    } ?>

    <?php if (!empty($formulario->extra)) {
        echo $formulario->extra;
    } ?>

  <div class="botonera">

    <button type="reset"><?= App\Core\View::e($formulario->borrar ?? 'recetear formulario') ?></button>
    <button type="submit"><?= App\Core\View::e($formulario->aceptar ?? 'aceptar') ?></button>
  </div>

  </form>
</div>