<?php
/** @var object $dato Contrato: id, tipo, valor, label, error, requerido. */
$datoId = $dato->id;
$tieneError = !empty($dato->error);
?>
<div class='inputs-span<?= $tieneError ? ' is-invalida' : '' ?>' key='<?= $datoId ?>'>
  <span><?= $dato->requerido ? '*' : '' ?></span>
  <div class='inputs-entero'>
    <input
      type="<?= App\Core\View::e($dato->tipo ?? 'text') ?>"
      id="<?= $datoId ?>"
      name="<?= $datoId ?>"
      value="<?= App\Core\View::e($dato->valor ?? '') ?>"
      placeholder=' '
    >
    <label
      for="<?= $datoId ?>"
    >
    <?= App\Core\View::e($dato->label) ?>
  </label>
  </div>
  <span><?= App\Core\View::e($dato->error ?? '') ?></span>
</div>